// https://www.11ty.dev/docs/config/

export default async function(eleventyConfig) {
};

export const config = {
  dir: {
    input: "src", // default: "."
    output: "dist", // default: "_site"
  }
};
