{
  "name": "turtlekeepers.org",
  "version": "1.0.0",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "sass": "^1.80.4",
    "terser": "^5.36.0"
  },
  "scripts": {
    "build": "npx @11ty/eleventy",
    "build:githubpages": "npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "private": true
}
